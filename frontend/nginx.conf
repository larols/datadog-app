user nginx;
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    client_header_buffer_size 16k;
    large_client_header_buffers 4 16k;

    include /etc/nginx/conf.d/*.conf;  # This line includes server configurations from conf.d
}